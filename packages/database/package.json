{
  "name": "@scheduler/database",
  "version": "1.0.0",
  "description": "PostgreSQL database layer, Prisma client, migrations, and atomic SKIP LOCKED query repositories",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "@scheduler/types": "^1.0.0",
    "@scheduler/shared": "^1.0.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "prisma": "^5.15.0",
    "typescript": "^5.4.5"
  }
}
